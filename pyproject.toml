# PRAXIS / RegPilot — the backend runs via PYTHONPATH=backend (see README / Makefile),
# so no installable-package configuration is needed; this file carries tooling config only.

[tool.pytest.ini_options]
pythonpath = ["backend"]
testpaths = ["backend/tests"]
filterwarnings = ["ignore::DeprecationWarning"]
