# PRAXIS — backend dependencies (Python 3.11)

# --- Agent orchestration + LLM ---
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-ollama>=0.2.0
ollama>=0.3.0

# --- RAG ---
sentence-transformers>=3.0.0
chromadb>=0.5.0
rank-bm25>=0.2.2

# --- Document parsing ---
pdfplumber>=0.11.0
pytesseract>=0.3.10
opencv-python-headless>=4.9.0
pillow>=10.0.0
beautifulsoup4>=4.12.0
requests>=2.31.0


# --- API + queue + DB ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
redis>=5.0.0
sqlalchemy>=2.0.0
alembic>=1.13.0
slowapi>=0.1.9
prometheus-fastapi-instrumentator>=7.0.0
psycopg2-binary>=2.9.9

# --- Config / models ---
pydantic>=2.7.0
pydantic-settings>=2.3.0

# --- Synthetic corpus + audit exports ---
reportlab>=4.2.0
openpyxl>=3.1.0

# --- CLI / utils ---
typer>=0.12.0
rich>=13.7.0

# --- Auth ---
python-jose[cryptography]>=3.3.0
# bcrypt>=4.1 removed the shim passlib 1.7.4's self-test relies on (raises "password
# cannot be longer than 72 bytes" on CryptContext init, breaking every login) — passlib
# has had no release since; pin below the break until one lands.
passlib[bcrypt]>=1.7.4
bcrypt<4.1
authlib>=1.3.0   # OIDC (SSO/Keycloak) authorization-code flow — the one integration dep
ldap3>=2.9.1

# --- Tests ---
pytest>=8.0.0
httpx>=0.27.0
